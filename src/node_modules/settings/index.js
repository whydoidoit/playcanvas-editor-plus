let settings = JSON.parse(localStorage.getItem('__ixion_plus_settings') || "{}")
settings.enabled = settings.enabled || {}

function save() {
    try {
        localStorage.setItem('__ixion_plus_settings', JSON.stringify(settings))
    } catch(e) {

    }
}
settings.save = save

export default settings
