function collect(node, nodes) {
    nodes.push(node)
    let children = node.get('children')
    for (let i = 0; i < children.length; i++) {
        let child = editor.call('entities:get', children[i])
        collect(child, nodes)
    }
    return nodes
}

export default collect
